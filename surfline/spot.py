# A class to define spots in Surfline's database.

class Spot:
    def __init__(self, spotName, spotId, camName):
        self.spotName = spotName
        self.spotId = spotId
        self.camName = camName